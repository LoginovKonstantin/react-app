import React, { Component } from 'react';
import { Link } from 'react-router-dom';

const Landing1 = () => (
  <div>
    Landing1
    <Link to={`/`} activeStyle={{ }}> Go home 1 </Link>
  </div>
);

export default Landing1;
