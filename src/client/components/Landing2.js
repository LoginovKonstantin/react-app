import React, { Component } from 'react';
import { Link } from 'react-router-dom';

const Landing2 = () => (
  <div>
    Landing2
    <Link to={`/`} activeStyle={{ }}> Go home </Link>
  </div>
);

export default Landing2;
